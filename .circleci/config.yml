version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.16.1
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Deploy to Droplet
          command: make circle-deploy
